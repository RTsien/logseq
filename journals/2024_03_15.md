- centos添加新用户 #linux
	- ```bash
	  #确定用户名
	  username=用户名
	  #新建用户
	  useradd $username
	  #更改用户密码，激活使用
	  passwd $username
	  # echo $username:your_new_password|chpasswd
	  #修改用户主目录
	  usermod -d /data/home/richieqian -u $(id -u $username) $username 
	  #添加sudoers
	  echo "$username    ALL=(ALL)       ALL" >> /etc/sudoers
	  ```