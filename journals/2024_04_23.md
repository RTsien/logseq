- [[RED/matchsvr]]压测记录
	- 从昨天开始[[RED/kadinwang]]一直出现压测娱乐赛正常匹配战斗逻辑，始终会出现同时战斗人数少于测试bot数量2000个的gap。
	  因为忙于分析另外一个问题，就没有直接深入到问题本身，而是继续调整bot数多压了几次，尝试压出bot数与同时战斗数的benchmark曲线，先看是不是一个线性的性能问题：
	  |-|-|-|
	  |bot数|同时战斗数|
	  |7500|5500|
	  |10000|7700|
	  |12000|9800|
	  然后[[silgryndeng]]确认matchsvr侧只要匹配成功的，最后都会创建对局。并且没有参与战斗的数量并没有随着bot数增加线性增长。所以就推断bot发送请求匹配的逻辑本身有问题。初步想查一下的线索就是看看是不是有一些bot从来没有参与过对局。然后在群里求助如何查出来这个数据，[[evanliang]]梁老师便导出了这个gid列表，总数量是2099，跟gap量级能够对上。随后梁老师反馈说bot这里有一个校验状态的bug，修复之后重新压测。
- [[RED]]什么时候会cherrypick
	- [一测发布实践复盘 - 腾讯iWiki (woa.com)](https://iwiki.woa.com/p/1071604509)
		- 看起来是从master分支挑出来版本mr到转测分支 #TODO
	- 怎么用
		- ```bash
		  git cherry-pick commit-hash // 合并特定提交哈希值
		  git add resolved-file // 如果有冲突，需要解冲突然后add到暂存区
		  git cherry-pick --continue
		  git push
		  ```
- [[MFCN/routing]] wasm server 是不是在 informer 初始化完成之后，才启动 api？
	- 代码地址[cmd/wasm-server/main.go - master - mfcn/routing - 工蜂内网版 (woa.com)](https://git.woa.com/mfcn/routing/blob/master/cmd/wasm-server/main.go)
	- 是的，看代码
	  ```go
	  func main() {
	  	......
	  	kubeCacheCtx, kubeCacheCancel := context.WithCancel(context.Background())
	  	go func() {
	  		err := kubeCache.Start(kubeCacheCtx)
	  		if err != nil {
	  			mlog.Fatal("failed to start kube cache", zap.Error(err))
	  		}
	  	}()
	  	kubeCache.WaitForCacheSync(kubeCacheCtx)
	      ......
	      httpServer := &http.Server{
	  		Addr:              fmt.Sprintf(":%d", *wasmPluginPort),
	  		Handler:           httpHandler,
	  		ReadHeaderTimeout: 5 * time.Second,
	  		WriteTimeout:      5 * time.Second,
	  	}
	  
	  	go func() {
	  		mlog.Infof("starting wasm plugin server")
	  		if err := httpServer.ListenAndServe(); err != nil && err != http.ErrServerClosed {
	  			mlog.Fatalf("wasm plugin server failed: %+v", err)
	  		}
	  	}()
	      ......
	  }
	  ```