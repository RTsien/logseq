- #常用工具 [CookieCloud](https://github.com/easychen/CookieCloud/blob/master/README_cn.md) 是一个和自架服务器同步Cookie的小工具，可以将浏览器的Cookie及Local storage同步到手机和云端，它内置端对端加密，可设定同步时间间隔。
	- 如何通过chrome插件获取网站cookie
		- `manifest.json`
		  ```json
		  {
		    "name": "__MSG_appTitle__",
		    "description": "__MSG_appDesc__",
		    "default_locale": "zh_CN",
		    "permissions": [
		      "cookies", // cookies权限
		      "tabs",
		      "storage",
		      "alarms",
		      "unlimitedStorage"
		    ],
		    "host_permissions": [
		      "<all_urls>"
		    ],
		    "version": "0.3.0"
		  }
		  ```
		- `extension/function.js`文件中
		  ```javascript
		  /**
		   * 从浏览器获取所有cookie
		   * @returns object
		   */
		  async function collect_cookie_from_browser()
		  {
		      const cookies_raw = await chrome.cookies.getAll({});
		      // ... (代码有删减，保留核心逻辑)
		      const cookies = {};
		      for( const item of cookies_raw )
		      {
		          // 忽略 httpOnly 的 cookie
		          if( item.httpOnly ) continue;
		          const domain = item.domain.replace(/^./, '');
		          if( !cookies[domain] )
		          {
		              cookies[domain] = [];
		          }
		          cookies[domain].push(item);
		      }
		      return cookies;
		  }
		  ```
			- **`if( item.httpOnly ) continue;`**
				- 这里有一个判断，如果 Cookie 是 `httpOnly` 的，就跳过。`httpOnly` 是一种安全设置，表示该 Cookie 不能被客户端脚本（如 JavaScript）访问。虽然插件的 API 可以读取到它们，但 `CookieCloud` 的作者在这里选择忽略它们，可能是因为这类 Cookie 主要用于服务器验证，同步到其他地方意义不大或存在安全风险。
			- 数据处理和归类
				- `const domain = item.domain.replace(/^./, '');`
				- `if( !cookies[domain] ) { cookies[domain] = []; }`
				- `cookies[domain].push(item);`
				- 这几行代码的作用是按域名对 Cookie 进行分组。例如，所有 `google.com` 的 Cookie 会被放在同一个数组里，所有 `github.com` 的 Cookie 会被放在另一个数组里，最终形成一个以域名为键、Cookie 列表为值的对象，方便后续处理（加密和上传）。
- 除了[[跳表]]]还有哪些支持范围查找的数据结构 #算法 #card
	- | 数据结构 | 主要优点 | 主要缺点 | 适用场景 |
	  | 跳表 (Skip List) | 实现相对简单，并发性能好（锁粒度小），综合性能优秀 | 内存开销比平衡树稍大（因为有冗余指针） | 通用内存索引，尤其是在需要频繁写和高并发读的场景。 |
	  | 红黑树 (Red-Black Tree) | 严格的$O(\log n)$复杂度，查询性能稳定，是标准库的实现 | 实现复杂，插入/删除的旋转操作可能影响高并发性能 | 通用内存索引，当需要一个稳定可靠的有序关联容器时。 |
	  | B+树 (B+ Tree) | 范围查询性能最佳，缓存友好性极高 | 实现最复杂，对于少量数据，其优势不明显 | 数据量巨大，需要极致优化范围查询和考虑CPU缓存效率的内存场景。 |
	  | 基数树 (Radix Tree) | 查询复杂度与数据量无关 (O(k))，对于固定长度键非常快 | 范围查询实现困难，空间效率依赖数据分布 | 键的长度固定且较短，对点查询速度有极致要求，例如IP路由表。 |
	  | 分段树 (Segment Tree) | 区间聚合查询（如计数、求和）极其高效 | 不适合返回区间内的具体元素，空间占用大，动态更新不便 | 需要对时间段进行聚合统计分析的场景。 |
- 更新[[logseq]]里面历史日期内容的时候，记得使用[[Sep 12th, 2025]]这样的格式标注一下，方便根据时间检索