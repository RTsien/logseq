- #RED/actsvr `entityWrapper.GetOpenedTimes()`,` actbase.GetCurrentDay(ctx, act.server, int64(act.ID()))` 活动在一天内先关后开，GetOpenedTimes加1，GetCurrentDay是1
	- 按理说活动关闭之后，entity应该会被清理，但为什么GetOpenedTimes会记录到打开次数
		- 1. OpenedTimes 存储在 actData 的元数据 Meta 中，而不是 Entity 中。
		  2. 活动关闭时调用的 ResetActData 方法，只会重置 Entity 的数据，并不会重置 actData.Meta
		- ```css
		    player.Player
		    └── player.ActData (每个活动一个)
		        ├── Meta (*commonpb.ActData_Meta)
		        │   ├── BeginTs      // 开始时间
		        │   ├── DailyClearTs // 每日重置时间
		        │   ├── ActState     // 活动状态
		        │   └── OpenedTimes  // 活动打开次数
		        │
		        └── Entity (具体活动的数据结构, e.g., *act0131.Entity)
		            ├── CurrentDay
		            ├── TaskStates
		            └── ... (其他活动相关的具体数据)
		  ```
-