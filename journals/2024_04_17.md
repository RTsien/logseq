- [[gomega]]可以使用同时对error是否是nil和err.Error()内容断言
	- ```golang
	  gomega.Expect(err).To(gomega.SatisfyAny(gomega.Succeed(), gomega.MatchError("rpc error: code = InvalidArgument desc = mongo: no documents in result")))
	  ```