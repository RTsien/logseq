- [[RED/actsvr]] h5分享图开发 from [[RED/wzsyfzhang]]：
	- 一般好友分享由客户端调起qq/wx，只需要在Share/ShareArkConfig里配置好token，后台不需要特别开发。
	  除非针对qq希望调起游戏内好友列表，直接发消息，就需要后台先调用[[RED/zonesvr]]上面的SendShareArk接口，参考活动69
	  ```go
	  	// 设置已发起召回状态
	  	entityWrapper.UpdateStatus(peerOpenID, entity.RecallStatus_RECALLING, now.Unix())
	  	// qq类型通过后端接口发送分享消息
	  	if r.BasicInfo().GetPlatformType() == int64(corepb.PlatformType_QQ) && pc.BasicConf.GetShareArkId() != 0 {
	  		go func(ctx context.Context) {
	  			ctx, cancel := context.WithTimeout(ctx, time.Second*3)
	  			defer cancel()
	  			if _, err := act.server.ZoneSendShareArk(zonepb.WithTargetFromSession(ctx), &sssharepb.SendShareArkRequest{
	  				ArkId:      pc.BasicConf.GetShareArkId(),
	  				PeerOpenid: peerOpenID,
	  			}); err != nil {
	  				mlog.Ctx(ctx).Errorf("send qq share ark failed: %v", err)
	  			}
	  		}(util.ValueOnlyContext{Context: ctx})
	  	}
	  ```
	- 调用潘多拉接口，获取分享链接，参考活动84、88