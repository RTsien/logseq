- 排查压测产生的matchsvr慢查询，插入匹配node的时候没有索引
	- ```go
	  func (mm *MongoManager) AddMatchNode(ctx context.Context, businessType, businessID int64, nodeID uint64, node interface{}) error {
	      col := mm.DbInst.Collection(fmt.Sprintf("entity_%d_%d", businessType, businessID))
	      _, err := col.UpdateOne(context.Background(), bson.M{
	          "id": nodeID,
	      }, bson.M{
	          "$set":         node,
	          "$currentDate": bson.M{"update_at": true},
	          "$inc":         bson.M{"private_id": 1},
	      }, options.Update().SetUpsert(true))
	      if err != nil {
	          mlog.Ctx(ctx).With(zap.String("match_shard", "AddMatchNode")).Warnf("AddMatchNode in mongo failed, businessType:%v, businessID:%v, node id:%v, err:%v", businessType, businessID, nodeID, err)
	          return util.ErrorWithPos(err)
	      }
	      return nil
	  }
	  ```
	- 加索引是定时的，的确有可能出现在一小段时间内写数据时没有索引的情况
	- ```go
	  // ReconcileEntity 将qm管理的queue与db中的锁对齐
	  func (qm *QueueMgr) ReconcileEntity() {
	      ......
	  	for _, lock := range AvailableEntity {
	            ......
	  
	        	if hasInit {
	  			continue
	  		}
	            ......
	  
	  		col := qm.shard.MongoManager.DbInst.Collection(fmt.Sprintf("entity_%d_%d", lock.BusinessType, lock.BusinessID))
	  		_, err = col.Indexes().CreateMany(context.Background(), []mongo.IndexModel{
	  			{Keys: bson.D{{Key: "id", Value: 1}}},
	  			{Keys: bson.D{{Key: "cancel_id", Value: 1}}},
	  		})
	  	}
	      ......
	  }
	  ```