- #RED/zonesvr gm清除用户数据的时候报错
	- gm failed: code=-1 rpc error: code = DeadlineExceeded desc = downstream duration timeout @internal/pkg/systems/rankproxy/impl/system.go:356 @internal/pkg/systems/lobby/gm/gm.go:254 @pkg/mod/git.woa.com/mfcn/gm-gateway/pkg@v0.1.4-0.20240618115246-e67216c709d8/gmapi/server.go:137
	- 但是在ranksvr里通过zonesvr log的header里的traceid(5c1b02f6b75e7ca5274aea0de6bd8abc)没办法直接关联到，是通过`__TAG__.namespace:"branch-test-main" AND ranksvr AND "/rankpb.RankService/Delete"`结合GID和时间才关联到的，它的traceid是(1a8089b83e1a9f76142d88e89335e852)
	- 这里有个问题，问什么gm调用的traceid没有继续向下传递？？？#TODO #traceid传递
- #RED/feato #RED/guildsvr 添加system级特性开关
	- 之前已经存在了playerToggle
	  ```go
	  type CoreSystem struct {
	  	redlock.RWMutex
	  
	  	systems      map[string]core.System           // 系统名称 -> 系统实例，所有注册的系统
	  	initializers map[string]Initializer           // 系统名称 -> 系统初始化函数，所有注册的系统初始化函数
	  	afterHookers map[string][]AfterSystemInitExec // 系统名称 -> 系统初始化后执行的 after hook 函数
	  
	  	region                                  *shard.Region                // region，管理数据迁移相关
	  	server                                  *grpc.Server                 // grpc server
	  	currentConfigPurveyor                   msconfig.Purveyor            // 当前配置提供者
	  	currentConfigPurveyorSubscriberProfiles []msconfig.SubscriberProfile // 当前配置提供者的订阅者配置
	  	ec                                      *eventcenter.Center          // 事件中心
	  	traceidGenerator                        *gametraceid.Generator       // game trace id 生成器
	  	tm                                      *timermgr.TimerMgr           // 定时任务管理器
	  	playerToggle                            redfeato.PlayerToggleInterface
	  ++	systemToggle                            feato.ToggleInterface
	  }
	  ```
	  实际在BasicSystem里封装，用来判断某一个操作是否被允许
	  ```go
	  func (s *BasicSystem) isOperationDisableByFeato(ctx context.Context, operationKey string) error {
	  	if ok, _ := system.PlayerToggle().Enabled(redfeato.PlayerContext{}, operationKey); !ok {
	  		return nil
	  	}
	  
	  	variants := system.PlayerToggle().GetAllMatchedVariants(redfeato.PlayerContext{}, operationKey)
	  	var startTime, endTime time.Time
	  ```
-
-
-
-