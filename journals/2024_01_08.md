- **14:48** [[quick capture]]: I really love walking my daughter to the bus stop in the morning. It's so refreshing to start the morning with a nice walk, especially on days it's kind of cool outside
-
- #外语学习 如何通过听读提升语料储备  #card
  id:: 659e5133-79bf-45dc-914b-ac39b6b63ac3
	- 优先听读简短，并且自己感兴趣的语料，比如cambly tutor Tracey推荐的[Engoo Daily News](https://engoo.com/app/daily-news) 和 [ELLLO](https://www.elllo.org/)，再配合着[每日英语听力](((65606b24-f0fa-4655-8e89-276cd6ae8725))) #engoo #elllo #每日英语听力
	  id:: 659b9f37-1ae7-4678-9f4d-3c42c294a47a
	- 要把听读预料过程中遇到的好的句子记录到[[logseq]]，并且打上 `英语` 和 `card` 标签，同步到[[anki]]定期复习
	  id:: 659b9dbc-e180-4043-b8a8-c7d44ab70acd
	- 要贯彻[刻意练习](((65606b24-b567-4e5f-ae5d-be97fd21aa00)))原则：
		- 学习区联系：选择合适自己的难度（**intermediate**）
		- 定期重复训练：[[anki]]
		- 持续获得有效的反馈：与[[cambly]] tutor/ [[ChatGPT]]讨论听读过的主题
		- 集中精力：采用短小的材料，在通勤环境下带降噪耳机学习
- 获取 [[golang]] 运行时信息 [zlsgo/zpprof/systeminfo.go at master · sohaha/zlsgo (github.com)](https://github.com/sohaha/zlsgo/blob/master/zpprof/systeminfo.go)
	- ```golang
	  func NewSystemInfo(startTime time.Time) *SystemInfo {
	  	var afterLastGC string
	  	mstat := &runtime.MemStats{}
	  	runtime.ReadMemStats(mstat)
	  	costTime := int(time.Since(startTime).Seconds())
	  	if mstat.LastGC != 0 {
	  		afterLastGC = fmt.Sprintf("%.1fs", float64(time.Now().UnixNano()-int64(mstat.LastGC))/1000/1000/1000)
	  	} else {
	  		afterLastGC = "0"
	  	}
	  
	  	serverName, _ := os.Hostname()
	  
	  	return &SystemInfo{
	  		ServerName:   serverName,
	  		Runtime:      fmt.Sprintf("%d天%d小时%d分%d秒", costTime/(3600*24), costTime%(3600*24)/3600, costTime%3600/60, costTime%(60)),
	  		GoroutineNum: strconv.Itoa(runtime.NumGoroutine()),
	  		CPUNum:       strconv.Itoa(runtime.NumCPU()),
	  		HeapInuse:    zfile.SizeFormat(uint64(mstat.HeapInuse)),
	  		UsedMem:      zfile.SizeFormat(uint64(mstat.Alloc)),
	  		TotalMem:     zfile.SizeFormat(uint64(mstat.TotalAlloc)),
	  		SysMem:       zfile.SizeFormat(uint64(mstat.Sys)),
	  		Lookups:      strconv.FormatUint(mstat.Lookups, 10),
	  		Mallocs:      strconv.FormatUint(mstat.Mallocs, 10),
	  		Frees:        strconv.FormatUint(mstat.Frees, 10),
	  		LastGCTime:   afterLastGC,
	  		NextGC:       zfile.SizeFormat(uint64(mstat.NextGC)),
	  		PauseTotalNs: fmt.Sprintf("%.3fs", float64(mstat.PauseTotalNs)/1000/1000/1000),
	  		PauseNs:      fmt.Sprintf("%.3fs", float64(mstat.PauseNs[(mstat.NumGC+255)%256])/1000/1000/1000),
	  	}
	  }
	  ```