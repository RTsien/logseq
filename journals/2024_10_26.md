- #RED/海域探险 FinishEvent的流程
	- 日清周清检查
	- 检查要完成的剧情是否是当前剧情
		- 如果是假事件
			- 假事件定义
			  ```go
			  var FakePlotMap = map[int64]*FakePlotDetail{
			  	// 用于地图解锁通知
			  	1: {
			  		PlotDetail: &voyageexplorepb.PlotDetail{
			  			PlotId:    1,
			  			EventId:   1,
			  			EventType: voyageexplorepb.EventType_GENERAL_EVENT,
			  		},
			  		Event: &CanUnlockMapEvent{},
			  	},
			  	// 用于中断飞手附着，必定成功
			  	2: {
			  		PlotDetail: &voyageexplorepb.PlotDetail{
			  			PlotId:    2,
			  			EventId:   1,
			  			EventType: voyageexplorepb.EventType_GENERAL_EVENT,
			  		},
			  		Event: &AttachmentStopEvent{},
			  	},
			  }
			  ```
			- 直接完成事件
		- 如果不是当前剧情
			- 并且不是快捷event
				- (哪些case？战斗结束提前发奖)
				- 先返回buffer的剧情结果
			- 如果是快捷event
				- 走正常的TryFinish
		- 如果是当前剧情
			- 走正常的TryFinish
	- TryFinish的流程
		- 如果是快捷通用event
		  collapsed:: true
			- 保存在队列中的已经开启的通用事件，可以直接完成 (不占用 CurrentPlot)
			- ShortcutFinishEvent
		- 如果不是快捷通用event
		  collapsed:: true
			- 走TryFinishEvent
- #RED/海域探险 切换地图的时候current_plot是否会被清空
	- TryFinish的时候会在剧情处理完后清空，前提是Finish的plot id跟current_plot的id相等
	- StartPlotWithConf的时候会通过EventDetail进行覆盖
		- **切地图**之后进入海域，会检查pending状态的剧情，如果有的话会重新StartPlotWithConf，会覆盖current_plot
- #RED/海域探险 进入海域的时候，会通过TryAgain把pending的一个普通事件和多个通用事件通过NotifyNewPlot推给客户端，每个事件会调用一次NotifyNewPlot
	- 参考 ((671a6f08-08fe-4904-bffb-741df6973cdc))
- #RED/海域探险 触发剧情的时候，如果触发的是其他地图会出现什么情况
	- 后台触发 triggerStartPlotWithConf
		- Do**Xxx**Trigger -> triggerStartPlotWithConf -> EnqueuePendingPlot
			- 如果触发的是别的地图的剧情，是不会在下面的executeQueueNextPlot里取出来
			- -> executeQueueNextPlot
				- 只会取出公共剧情地图和当前地图的pending剧情
				- 通用剧情
					- 不会调用StartPlotWithConf，直接记录到**TriggeredGeneralSuccPlotID**
					- 所以**TriggeredGeneralSuccPlotID**记录的全部都是后台触发的通用剧情
						- 与之相对的是前台触发的 {{embed ((671ceb39-be4c-4097-9d8c-2f8ddd986790))}}
						- 💡不管**TriggeredGeneralSuccPlotID**还是**PendingGeneralPlots**，都被认为时**IsShortcutPlot**
				- 普通剧情
					- executeQueueNextPlot里调用StartPlotWithConf，不会对物件状态进行检查
					- executeQueueNextPlot和CurrentPlot的关系是什么❓
		- 进入海域的时候执行PeekTriggeredPlotQueueAndNotify
			- PeekTriggeredPlotQueueAndNotify里调用StartPlotWithConf，不会对物件状态进行检查
			- 但是如果是其他地图后台触发的当前地图的通用剧情，会在进入海域的时候被清理掉❗
	- 前台触发 StartPlot
		- 如果是技能触发，StartPlotWithSkill也不会检查物件位置
		- 如果是物件状态触发
			- 如果物件不在当前地图，肯定会被后端拦住
			- 如果物件在当前地图，会直接在当前地图触发
				- 通用剧情会记录在plot对应地图的pending上：**PendingGeneralPlots**
				  id:: 671ceb39-be4c-4097-9d8c-2f8ddd986790
				- 普通剧情会调用EventDetail，记录到CurrentPlot上
					- 如果客户端切换地图或者杀后台，这个记录会丢失
					- 如果客户端/后台触发了一个普通剧情，会把CurrentPlot覆盖
					- 这段代码很奇怪，IsFinished是针对CurrentPlot的，是怎么确信传入的Plot就是CurrentPlot呢❓
					  ```go
					  func (s *VoyagExploreSystem) StartPlotWithConf(ctx context.Context, r *player.Player, pc *config.ParsedConfig,
					  	plotCfg *config.PlotDefine, pos int64, isTriggered bool, sourceComponentID int64, targetComponentIDs []int64) (*voyageexplorepb.PlotDetail, error) {
					  	rModel := mustPlayerModel(r)
					  
					  	// 1. 检查事件是否已经完成了，完成则不再触发
					  	if !isTriggered && rModel.IsFinished(ctx) {
					  		// 防刷等场景: 已经有完成的事件并且尝试重新开启一次事件
					  		mlog.Ctx(ctx).Warn("StartPlotWithConf, player has finished event")
					  		return nil, util.ErrorWithPos(voyageexplore.ErrPlayerHasFinishedEvent)
					  	}
					  ```
						- 目前看来只有在副本结算的时候才有rModel.SetFinished(ctx, r, true)的逻辑
						- 随后会立即尝试完成事件和整个剧情，如果成功完成就会把CurrentPlot清除
						- 所以只有在副本结算成功，但是事件或者剧情结束失败的时候才会有rModel.IsFinished(ctx)的情况，而且这个时候不管StartPlot传入的plot是什么都不会被挡住。这回造成一个问题：本应该可以同时触发的通用剧情被挡住了❗
						- #TODO 这里要考虑如何修复：是不是通用剧情就不走IsFinished检查
						-
- #RED/海域探险 客户端触发的普通事件是否实现排队？
	- {{embed ((671bba81-fe43-4987-acd9-7c8316a57782))}}
	- 对于客户端触发的普通事件没有排队机制，新StartPlot的普通事件会覆盖旧的CurrentPlot