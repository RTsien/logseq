- [[RED/matchsvr]]的shard和queue的边界条件
	- `core/match`business的实现
		- `core/match/result_handle.go`
			- `MATCH_RESULT_SIZE = 1000` 结果处理队列的chan长度
				- ```go
				  func NewMQResultHandler(mqClient *pubsub.Client) *MQResultHandler {
				  	return &MQResultHandler{
				  		mqClient: mqClient,
				  		results:  make(chan *Result, MATCH_RESULT_SIZE),
				  	}
				  }
				  ```
				- 消费(结果)处理得慢的话，会阻塞发送方，发送方具体是谁
	- `core/model`queue的实现
	- `core/shard`shard的实现
- [[RED/matchsvr]]为什么用grpcResultHandler替换掉mqResultHandler
	- mq是个相对新的组件，用grpc更好控制质量
	  logseq.order-list-type:: number
	- grpc做trace更方便，更好掌控全链路
	  logseq.order-list-type:: number
- 补充[[RED/压测]]相关资料