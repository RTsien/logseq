- #RED/zonesvr 赛年任务配置在PvpYear.xlsx，最大进度是5星，也就是5次
  ```go
  maxProgress := int64(0)
  for _, s := range task.Star {
  	maxProgress = max(maxProgress, s.MaxProgress)
  }
  ```
- #RED/zonesvr 段位赛赛季未开启时，不会加减段位分。PvpLadder/LadderSeason表控制赛季时间。Pvp1v1GameOver tlog可以查到段位分变化。
  ```go
  // 计算开始时所处的赛季阶段
  _, seasonStage := s.ParsedConfig().LadderSeasonConfigByTime(time.Unix(startTime, 0))
  - // 如果开始时所处的赛季阶段为休赛期，或者已经跨赛季，那么不进行积分结算
  if seasonStage == config.LadderSeasonStageOff || startSeasonID != m.SeasonID() {
  mlog.Ctx(ctx).Infof("ladder settle score while season stage off, start time: %v, start season: %v", startTime, startSeasonID)
  // 双方积分计算 用于客户端结算界面展示
  for _, info := range fightInfos {
  	result.TeamInfo = append(result.TeamInfo, &zonepb.PvPLadderGameOverMsg_LadderTeamInfo{
  		Score: info.FightExt.GetLadder().GetScoreInfo().GetScore(),
  	})
  }
  } 
  ```